import{getContext}from"@/lib/context";import{PageHeader}from"@/components/page-header";import{BillingActions}from"@/components/billing-actions";
export default async function Billing(){const{db,member}=await getContext(),oid=member.org_id;const[{data:org},{data:usage},{count:seats}]=await Promise.all([db.from("organizations").select("*").eq("id",oid).single(),db.from("usage_events").select("module,quantity").eq("org_id",oid),db.from("org_members").select("*",{count:"exact",head:true}).eq("org_id",oid)]);const totals=(usage||[]).reduce((a:any,x:any)=>(a[x.module]=(a[x.module]||0)+x.quantity,a),{});return <><PageHeader eyebrow="Plans & usage" title="Billing that grows with you"><BillingActions hasCustomer={!!org?.stripe_customer_id}/></PageHeader><div className="grid gap-5 lg:grid-cols-[.8fr_1.2fr]"><section className="rounded-2xl bg-ink p-7 text-white"><p className="text-xs font-bold uppercase tracking-widest text-lime">{org?.subscription_status||"trialing"}</p><h2 className="mt-5 text-3xl font-black">ERICA Growth</h2><p className="mt-2 text-white/50">$19 per seat + workflow usage</p><div className="mt-10 border-t border-white/10 pt-5"><p className="text-sm text-white/45">Trial ends</p><b>{org?.trial_ends_at?new Date(org.trial_ends_at).toLocaleDateString():"—"}</b></div></section><section className="card"><h2 className="text-xl font-black">Current usage</h2><div className="mt-6 grid grid-cols-2 gap-3"><div className="rounded-xl bg-black/5 p-4"><p className="text-xs text-black/45">Seats</p><b className="text-2xl">{seats||1}</b></div>{Object.entries(totals).slice(0,5).map(([k,v])=><div className="rounded-xl bg-black/5 p-4" key={k}><p className="text-xs capitalize text-black/45">{k.replaceAll("_"," ")}</p><b className="text-2xl">{v as number}</b></div>)}</div></section></div></>}
