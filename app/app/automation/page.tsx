import{getContext}from"@/lib/context";import{PageHeader}from"@/components/page-header";import{AutomationActions}from"@/components/module-actions";import{Bot,CheckCircle2,GitBranch,Zap}from"lucide-react";
const labels:Record<string,string>={"inventory.low":"Stock falls below reorder point","deal.won":"Deal moves to Won","leave.approved":"Leave request is approved",draft_po:"Draft PO + notification",customer_invoice:"Customer + invoice",leave_calendar:"Notify + calendar block"};
export default async function Automation(){const{db,member}=await getContext(),oid=member.org_id;const[{data:rules},{data:runs}]=await Promise.all([db.from("automation_rules").select("*").eq("org_id",oid).order("created_at"),db.from("automation_runs").select("*,automation_rules(name)").eq("org_id",oid).order("created_at",{ascending:false}).limit(12)]);return <><PageHeader eyebrow="Automation · Visual workflows" title="Make work flow itself"><AutomationActions rules={rules||[]}/></PageHeader><div className="rounded-2xl bg-lime/40 p-5"><div className="flex gap-3"><Zap className="text-moss"/><div><b>Trigger → condition → action, across every module.</b><p className="mt-1 text-sm text-black/55">Each workflow creates real records, meters usage, and writes an auditable run.</p></div></div></div><div className="mt-6 grid gap-4 lg:grid-cols-3">{rules?.map(r=><div className="card" key={r.id}><div className="flex items-center justify-between"><span className="grid h-10 w-10 place-items-center rounded-xl bg-ink text-lime"><Bot size={18}/></span><span className={`rounded-full px-2 py-1 text-xs font-bold ${r.enabled?"bg-green-100 text-green-800":"bg-black/5"}`}>{r.enabled?"Active":"Paused"}</span></div><h3 className="mt-5 text-lg font-black">{r.name}</h3><div className="mt-4 space-y-2 text-sm"><p className="rounded-xl bg-black/5 p-3"><b>WHEN</b> · {labels[r.trigger_key]||r.trigger_key}</p>{Object.keys(r.condition||{}).length>0&&<p className="rounded-xl bg-black/5 p-3"><b>IF</b> · {r.condition.field} {r.condition.operator} {String(r.condition.value)}</p>}<p className="rounded-xl bg-black/5 p-3"><b>THEN</b> · {labels[r.action_key]||r.action_key}</p></div><p className="mt-4 text-xs text-black/40">{r.runs} successful runs</p></div>)}</div><section className="card mt-6"><h2 className="text-xl font-black">Execution history</h2><div className="mt-4 divide-y">{runs?.length?runs.map(r=><div className="flex items-center gap-3 py-3" key={r.id}>{r.status==="success"?<CheckCircle2 className="text-green-600" size={18}/>:<GitBranch className="text-red-600" size={18}/>}<div><p className="text-sm font-bold">{r.automation_rules?.name||"Automation"} · {r.status}</p><p className="text-xs text-black/40">{new Date(r.created_at).toLocaleString()}{r.error?` · ${r.error}`:""}</p></div></div>):<p className="py-8 text-center text-sm text-black/40">Workflow runs will appear here.</p>}</div></section></>}
