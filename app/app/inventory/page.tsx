import{getContext}from"@/lib/context";import{PageHeader}from"@/components/page-header";import{RecordForm}from"@/components/record-form";import{StockAction}from"@/components/stock-action";import{AlertTriangle,PackageCheck}from"lucide-react";import{money}from"@/lib/utils";
export default async function Inventory(){const{db,member}=await getContext(),oid=member.org_id;const[{data:items},{data:assets},{data:moves}]=await Promise.all([db.from("inventory_items").select("*").eq("org_id",oid).order("name"),db.from("assets").select("*,employees(entities(name))").eq("org_id",oid),db.from("stock_movements").select("*,inventory_items(name)").eq("org_id",oid).order("created_at",{ascending:false}).limit(8)]);return <><PageHeader eyebrow="Inventory & assets" title="Know what’s where"><div className="flex gap-2"><StockAction items={items||[]}/><RecordForm table="inventory_items" button="New item" fields={[{name:"name",label:"Item name",required:true},{name:"sku",label:"SKU",required:true},{name:"quantity",label:"Opening quantity",type:"number"},{name:"reorder_point",label:"Reorder at",type:"number"},{name:"unit_cost",label:"Unit cost",type:"number"}]}/></div></PageHeader><div className="grid gap-4 lg:grid-cols-3">{items?.map(i=>{const low=Number(i.quantity)<=Number(i.reorder_point);return <div className="card" key={i.id}><div className="flex justify-between"><PackageCheck className="text-moss"/>{low&&<span className="flex items-center gap-1 text-xs font-bold text-amber-700"><AlertTriangle size={14}/> Low</span>}</div><h3 className="mt-6 text-lg font-black">{i.name}</h3><p className="text-xs text-black/40">{i.sku}</p><div className="mt-5 flex justify-between"><b>{i.quantity} {i.unit}</b><span className="text-sm text-black/50">{money(i.unit_cost)} each</span></div></div>})}</div><div className="mt-6 grid gap-6 lg:grid-cols-2"><section className="card"><h2 className="text-xl font-black">Recent movements</h2>{moves?.map(m=><div className="flex justify-between border-b py-3 text-sm" key={m.id}><span>{m.inventory_items?.name}</span><b className={m.direction==="in"?"text-green-700":"text-red-600"}>{m.direction==="in"?"+":"-"}{m.quantity}</b></div>)}</section><section className="card"><h2 className="text-xl font-black">Company assets</h2>{assets?.length?assets.map(a=><div className="flex justify-between border-b py-3 text-sm" key={a.id}><b>{a.name}</b><span>{a.employees?.entities?.name||a.status}</span></div>):<p className="py-8 text-sm text-black/40">No assets checked out yet.</p>}</section></div></>}
