import Stripe from"stripe";import{NextResponse}from"next/server";import{createClient}from"@/lib/supabase/server";
export async function POST(){const db=createClient(),{data:{user}}=await db.auth.getUser();if(!user)return NextResponse.json({},{status:401});const{data:m}=await db.from("org_members").select("organizations(stripe_customer_id)").eq("user_id",user.id).single(),id=(m?.organizations as any)?.stripe_customer_id;if(!id)return NextResponse.json({error:"Start a subscription first"},{status:400});const s=await new Stripe(process.env.STRIPE_SECRET_KEY!).billingPortal.sessions.create({customer:id,return_url:`${process.env.NEXT_PUBLIC_APP_URL}/app/billing`});return NextResponse.json({url:s.url})}
