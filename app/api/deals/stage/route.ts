import { NextResponse } from "next/server";import { createClient } from "@/lib/supabase/server";import { runAutomations } from "@/lib/automation";
export async function POST(req:Request){const {id,stage}=await req.json(),db=createClient(),{data:{user}}=await db.auth.getUser();if(!user)return NextResponse.json({}, {status:401});const {data:m}=await db.from("org_members").select("org_id").eq("user_id",user.id).single();const {data:deal,error}=await db.from("deals").update({stage}).eq("id",id).eq("org_id",m!.org_id).select("*,entities(*)").single();if(error)return NextResponse.json({error:error.message},{status:400});if(stage==="Won")await runAutomations(db,m!.org_id,"deal.won",deal);return NextResponse.json(deal)}
