"use client";import{useState}from"react";export function BillingActions({hasCustomer}:{hasCustomer:boolean}){const[busy,setBusy]=useState(false);async function go(path:string){setBusy(true);const r=await fetch(path,{method:"POST"}),x=await r.json();if(x.url)location.href=x.url;else{alert(x.error||"Unable to continue");setBusy(false)}}return <div className="flex gap-2"><button disabled={busy} className="btn" onClick={()=>go("/api/billing/checkout")}>Choose plan</button>{hasCustomer&&<button disabled={busy} className="btn-secondary" onClick={()=>go("/api/billing/portal")}>Billing portal</button>}</div>}
