"use client";export function InvoicePDF({invoice}:{invoice:any}){return <button className="text-xs font-bold text-moss" onClick={async()=>{const{jsPDF}=await import("jspdf");const doc=new jsPDF();doc.setFontSize(26);doc.text("ERICA INVOICE",20,25);doc.setFontSize(11);doc.text(`Invoice: ${invoice.invoice_number}`,20,42);doc.text(`Customer: ${invoice.entities?.name||""}`,20,50);doc.text(`Due: ${invoice.due_on||"On receipt"}`,20,58);doc.line(20,68,190,68);doc.setFontSize(18);doc.text(`Total: $${Number(invoice.amount).toFixed(2)}`,20,84);doc.save(`${invoice.invoice_number}.pdf`)}}>Export PDF</button>}
