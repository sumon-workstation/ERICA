"use client";import{useState}from"react";
export function HrActions({employees}:{employees:any[]}){const[mode,setMode]=useState("");async function submit(e:any){e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/hr",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({mode,data:f})});if(r.ok)location.reload();else alert((await r.json()).error)}return <><div className="flex gap-2"><button className="btn-secondary" onClick={()=>setMode("employee")}>Add employee</button><button className="btn" onClick={()=>setMode("leave")}>Request leave</button></div>{mode&&<div className="fixed inset-0 z-50 grid place-items-center bg-black/40" onClick={()=>setMode("")}><form onSubmit={submit} onClick={e=>e.stopPropagation()} className="w-full max-w-md space-y-3 rounded-2xl bg-cream p-6"><h2 className="text-2xl font-black">{mode==="employee"?"Add employee":"Request leave"}</h2>{mode==="employee"?<><input name="name" className="input" placeholder="Full name" required/><input name="email" className="input" type="email" placeholder="Email"/><input name="job_title" className="input" placeholder="Job title"/><input name="department" className="input" placeholder="Department"/><input name="start_date" className="input" type="date"/></>:<><select name="employee_id" className="input">{employees.map(e=><option value={e.id} key={e.id}>{e.entities?.name}</option>)}</select><select name="leave_type" className="input"><option>Vacation</option><option>Sick</option><option>Personal</option></select><input name="starts_on" className="input" type="date" required/><input name="ends_on" className="input" type="date" required/><input name="reason" className="input" placeholder="Reason"/></>}<button className="btn w-full">Save</button></form></div>}</>}
export function ApproveLeave({id}:{id:string}){return <button className="text-xs font-bold text-moss" onClick={async()=>{await fetch("/api/hr",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({id,status:"approved"})});location.reload()}}>Approve</button>}
