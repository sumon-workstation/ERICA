"use client";import {useState} from "react";
export function StockAction({items}:{items:any[]}){const [open,setOpen]=useState(false);async function save(e:any){e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/stock",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(f)});if(r.ok)location.reload();else alert((await r.json()).error)}return <><button className="btn-secondary" onClick={()=>setOpen(true)}>Stock in / out</button>{open&&<div className="fixed inset-0 z-50 grid place-items-center bg-black/40" onClick={()=>setOpen(false)}><form onSubmit={save} onClick={e=>e.stopPropagation()} className="w-full max-w-md space-y-3 rounded-2xl bg-cream p-6"><h2 className="text-2xl font-black">Stock movement</h2><select className="input" name="item_id">{items.map(i=><option value={i.id} key={i.id}>{i.name}</option>)}</select><select className="input" name="direction"><option value="in">Stock in</option><option value="out">Stock out</option></select><input className="input" name="quantity" type="number" min="1" placeholder="Quantity" required/><input className="input" name="reason" placeholder="Reason"/><button className="btn w-full">Record movement</button></form></div>}</>}
